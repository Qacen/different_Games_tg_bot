package org.example;

import org.telegram.telegrambots.meta.api.objects.Update;

public class cmd {
    public void processing(String command, Update update){
        if (command.equals("/joinRoom")){

        } else if (command.equals("/createRoom")) {

        } else if (command.equals("/deleteRoom")) {

        }
    }
}
